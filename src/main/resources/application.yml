server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  config:
    import: application-dev.yml

  servlet:
    multipart:
      max-file-size: 10MB    # 파일당 최대 크기
      max-request-size: 10MB # 전체 요청 최대 크기

